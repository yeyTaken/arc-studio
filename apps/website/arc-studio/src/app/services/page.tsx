'use client';

import { Button } from "@heroui/react";

export default function Page() {
  return (
    <div className="flex items-center justify-center h-screen">
      <Button color="primary" size="lg">Clique Aqui</Button>
    </div>
  );
}
